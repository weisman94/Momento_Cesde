<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="com.example.sagf">

    <dist:module dist:instant="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ListarFacturaActivity"></activity>
        <activity android:name=".NuevaFacturaActivity" />
        <activity android:name=".NuevoServicioActivity" />
        <activity android:name=".ListarServicioActivity" />
        <activity android:name=".ListarActivoActivity" />
        <activity android:name=".NuevoActivoActivity" />
        <activity android:name=".Menus.MenuFactura" />
        <activity android:name=".Menus.MenuServicio" />
        <activity android:name=".Menus.MenuActivos" />
        <activity android:name=".Menus.MenuUsuarioAActivity" />
        <activity android:name=".Menus.MenuActivity" />
        <activity android:name=".NuevoUsuarioActivity" />
        <activity android:name=".ListarUsuariosActivity" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".SplashActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>